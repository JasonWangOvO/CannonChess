# CannonChess Agent 工作规范

开始任何任务之前，必须优先阅读：

* `.codex/REQUIREMENTS.md`

---

# 项目说明

项目名称：

CannonChess

中文名称：

炮棋

开发语言：

Python 3.10

---

# 工作空间限制

Agent 的工作范围仅限于当前项目根目录。

允许：

* 读取项目内文件
* 创建项目内文件
* 修改项目内文件
* 执行安全的项目内命令

禁止：

* 访问项目目录之外的文件
* 修改用户主目录
* 修改系统配置
* 修改环境变量
* 修改全局 Python 配置
* 修改全局 Git 配置
* 修改 Shell 配置

任何超出项目目录的操作都必须视为禁止。

---

# 网络访问规则

禁止联网。

禁止：

* curl
* wget
* git clone
* 外部 API 请求
* 下载依赖
* 上传文件
* 访问任何外部服务

如果需要额外依赖：

向用户说明原因并请求批准。

---

# Git 管理规则

Git 完全由用户管理。

允许：

* git status
* git diff
* git log
* git show

允许通过 Git 信息理解：

* 当前版本状态
* 当前任务上下文
* 用户指定版本

禁止：

* git add
* git commit
* git push
* git pull
* git merge
* git rebase
* git reset
* git stash
* git checkout
* git switch
* 创建分支
* 删除分支

Agent 不负责任何提交操作。

---

# Python 环境

统一使用：

Python 3.10

优先：

* 标准库
* 项目现有依赖

除非用户明确要求，否则不要新增第三方依赖。

---

# 开发原则

1. 最小修改原则
2. 不修改无关代码
3. 保持向后兼容
4. 保持代码可测试
5. 游戏规则与界面分离
6. 游戏规则逻辑尽量保持纯函数化
7. 先理解需求再修改代码

---

# 工作流程

执行任务时：

1. 阅读需求
2. 阅读相关代码
3. 简述理解结果
4. 给出实施计划
5. 修改必要文件
6. 运行相关测试
7. 总结结果

总结必须包含：

* 修改文件
* 实现内容
* 测试结果
* 剩余风险

---

# 规则来源

炮棋规则唯一来源：

`.codex/REQUIREMENTS.md`

如果代码、历史实现或推测与需求冲突：

以 REQUIREMENTS 为准。

禁止自行创造规则。

---

# 用户协作原则

用户是项目负责人。

当需求存在歧义时：

* 给出合理推测
* 明确标记推测内容
* 请求用户确认

不要擅自扩大任务范围。

不要顺手重构无关代码。
